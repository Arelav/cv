---
name: tanstack-query
description: Look up TanStack Query docs via context7 before writing client-side data fetching
---

Use context7 with library ID `/tanstack/query` for TanStack Query docs.

Used for **client-side** data fetching only. Server Components fetch directly — no TanStack Query on the server.
